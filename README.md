# Notas:

Este es mi primer programa en node
```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Base de ala tabla de multiplicar            [number] [required]
  -l, --list     permite listar la tbla de multiplicar[boolean] [default: false]
  -h, --hasta    indica hasta que número se va a multiplicar
                                                          [number] [default: 10]
                                                          
```